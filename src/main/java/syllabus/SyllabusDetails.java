package syllabus;

public class SyllabusDetails {
/*
 * 1.Selenium tool history
 * 2.Selenium WebDriver version 3
 * 3.Selenium WebDriver version 4
 * 4. Java-Selenium Architecture
 * 5. Maven project - creation
 * 6. Adding Dependencies
 * 7. download exe file to use webdriver manager to launch browser(Sel vr3)
 * 8. webdriver -for sel ver4
 * 9. webdriver methods(1.get(String url) 2.getTitle 3. getCurrentUrl 4.getPageSource 5. navigate (fullScreen, maximize ,minimize ) 6.close 7.quit 
 * 10. Locators and types  (id, name, className, linkText, partialLinkText, cssSelector, xpath, tagName)
 * 11. Types of cssSelector (6) ---> 1.[AN='AV'] 2. htmltag[AN='AV'] 3. [#AV]--->only for id  4. [htmltag#AV]--->only for id   5. [.AV]--->only for className 6.htmltag[.AV]--->only for className
 * 12. Xpath (Types) --Absolute XPath , Relative XPath
 * Relative Xpath---->Basic Relative XPath  and Advanced Relative XPath
 * Basic Relative XPath:-xpath by attribute,  xpath by text,  xpath by contains Attribute, xpath by contains text
 * Advanced Relative XPath:- xpath by group index,  xpath by traversing
 * xpath by traversing:- Independent&dependent xpath , xpath by axes
 * Independent&dependent xpath:- forward traversing , backward traversing
 *  xpath by axes:- parent, child, siblings, ancestor, dependent
 * 
 * xpath by Multiple Attirbutes  (using AND , OR)
 * Aboslute xpath:-
 *  parent to immediate child-----> /
 *  parent to any child ----->//  
 *  child to parent-----> /.. 
 *  elder to younger-----> /following-sibling::htmltag[path]
 *  younger to elder-----> preceding-sibling::htmltag[path]
 *  
 * 13. Handle check-box  
 * 14. shortcuts to use in xpath to find image, link, tables, drop down, radio button, text-box
 * 15.WebElement and methods (sendKeys, clear, click, submit, getLocation, getSize, getRect, isDisplayed, isEnabled,
 * 							 isSelected, getAttribute, getDomAttribute, getProperty, getTagName, getArialRole, getAccessibleName, 
 * 							getText, cssValue, getScreenshot) 
 * 16. Handle Multiple WebElements
 * 17.How to freeze the screen
 * 18. Usage of Keyboard in Automation ----> KeysClass, Robot Class, Actions Class(Mouse over actions)
 * 			Mouse over Actions-->Click on Element, DoubleClick, Right click, Move to Element, Drag and Drop
 * 19. Drag and Drop Action
 * Handling Drop Down using Keys class
 * Handling Drop down using robot class
 * Handling Drop down using Select class
 * 			1>Single select drop down  2>SelectAll Drop down	3>Multiple-Drop down using Select
 * Handling Drop down using WebElement
 * Handling Scroll bar:-	1>using JavaScriptExecutor (1>true/false 2>JSE code)
 * 							2>scroll by amount(x,y axes)	3>scroll from origin	4>scroll to element(Web Element traget)	
 * 							5>scroll left or right, up or down (using jse(window.scrollTo))
 * 
 * Synchronization Handling (4 types) --- Java wait, ImplicitWait , ExplicitWait, FluentWait
 * Pop up:----> Authentication Pop up, Alert Pop up(Simple, Confirm, Prompt),Calendar pop up,File Upload pop up, 
 * 				IFrame Pop up, Notification pop up, Window Pop up (Single Window, Multiple/Child Window)
 * Frame Pop up -switching frame -3 approach
 *		1> Nested Frame	2>HIdden Division Pop up	3>traversing from child to parent ---use ancestor and descendant 
 *
 * taking Screenshot
 * StaleElementreferenceexception
 * SVG Handling
 * Calendar Select Date (with format) 
 * JavasriptExecutor ----> 1)used for scrolling	2)disabledLink to click 3) disabled text field   4) perform action usign jse
 * login to webpage without using actions class and webelement (use jse code)  ---->IMPORTANT INTERVIEW QUESTION
 * data driven testing ----> properties, excel files
 * pom
 * testng
 * assertion
 * batch execution
 * parallel execution
 * test reports
 */
	
	
	
}
